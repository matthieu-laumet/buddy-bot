<div class="modal-container">
  <div class="">
    <div class="">
      <%= simple_form_for([topic, batch, schedule]) do |f| %>
        <div>
          <div class="">
            <%= f.association :batch, label: 'Batch', label_method: :camp, placeholder: '733', required: true %>
          </div>
        </div>
        <div class="">
            <%= f.association :topic, label: 'Conversation', placeholder: '42', required: true %>
        </div>
        <div class="">
            <%= f.input :post_at, as: :date, html5: true, placeholder: 'date', required: true %>
        </div>
        <div class="">
        <%= f.button :submit, 'Créer!', class: 'btn btn-danger' %>
      <% end %>
        </div>
        <div>
          <span class="modal-close"><i class="fas fa-times"></i></span>
        </div>
    </div>
  </div>
</div>
